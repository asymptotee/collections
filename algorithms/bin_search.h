#ifndef BIN_SEARCH_H
#define BIN_SEARCH_H

/**
 * It takes an array of integers and the desired number + size of array
 * @return If it is found, its index is returned, if not, -1 is returned
*/
int bin_search(int[], int, int);

#endif